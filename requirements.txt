# Python 3.12. Pinned exactly; see CLAUDE.md "Conventions".
# Bump deliberately, never with unpinned ranges — the calculation path must be
# reproducible from stored inputs.

alpaca-py==0.43.5
pandas==2.2.3
numpy==2.1.3
scipy==1.14.1
pandas-market-calendars==5.4.0
python-dotenv==1.2.2

streamlit==1.40.2
plotly==5.24.1

pytest==8.3.4
