# Python 3.14. Pinned exactly; see CLAUDE.md "Conventions".
# Bump deliberately, never with unpinned ranges — the calculation path must be
# reproducible from stored inputs.

alpaca-py==0.43.5
pandas==3.0.5
numpy==2.5.1
scipy==1.18.0
pandas-market-calendars==5.4.0
python-dotenv==1.2.2

streamlit==1.60.0
plotly==6.9.0

pytest==9.1.1
