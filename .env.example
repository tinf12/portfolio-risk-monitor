# Copy to .env and fill in. .env is gitignored and must never be committed.
# Generate these at https://app.alpaca.markets/ under Paper Trading -> API Keys.

ALPACA_API_KEY=
ALPACA_SECRET_KEY=

# Optional. Defaults to data/risk.db relative to the repo root.
RISK_DB_PATH=
